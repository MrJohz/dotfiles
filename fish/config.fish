# Everything lives in ~/.config/fish/conf.d/, which fish reads before this file.
#
# This file mainly exists so that if a tool automatically tries to modify it, that
# will indicate that the dotfile is no longer in sync, and we can move that line into
# a more relevant location
